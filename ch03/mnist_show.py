# coding: utf-8
import sys, os
sys.path.append(os.pardir)  # 부모 디렉터리의 파일을 가져올 수 있도록 설정, sys.path.append(모듈을 저장한 디렉터리) 사용하기, os.pardir : 현재 디렉토리의 상위 디렉토리
import numpy as np
from dataset.mnist import load_mnist  # ..\dataset\mnist.py 파일에서 load_mnist 함수를 호출함
from PIL import Image


def img_show(img):
    pil_img = Image.fromarray(np.uint8(img))
    pil_img.show()

(x_train, t_train), (x_test, t_test) = load_mnist(flatten=True, normalize=False)

img = x_train[0]
label = t_train[0]
print(label)  # 5

print(img.shape)  # (784,)
img = img.reshape(28, 28)  # 형상을 원래 이미지의 크기로 변형
print(img.shape)  # (28, 28)

img_show(img)
